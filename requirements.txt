# ==============================================================================
# requirements.txt — Host machine dependencies (minimal)
#
# Everything else runs inside Docker containers:
#   - dbt          → scheduler container (make dbt-run / dbt-test / dbt-docs)
#   - flake8/black → scheduler container (make lint)
#   - dashboard    → dedicated dashboard container (make dashboard)
#   - Fernet key   → python:3.11-slim container (make airflow-fernet)
#
# Only `make test` runs locally, requiring the three packages below.
# Install with: pip install -r requirements.txt
# ==============================================================================

pytest==8.1.1
pandas==2.2.2
requests==2.31.0
