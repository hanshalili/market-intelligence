# ==============================================================================
# requirements.txt — Host machine / CI environment dependencies
#
# For running scripts locally (outside Docker), linting, and Terraform.
# The Airflow container has its own requirements at airflow/requirements.txt.
# ==============================================================================

# GCP
google-cloud-storage==2.16.0
google-cloud-bigquery==3.20.1
google-cloud-bigquery-storage==2.25.0
pyarrow==15.0.2
db-dtypes==1.2.0

# HTTP
requests==2.31.0

# Data processing
pandas==2.2.2

# dbt
dbt-core==1.7.14
dbt-bigquery==1.7.9

# Dev / CI
python-dotenv==1.0.1
pytest==8.1.1
flake8==7.0.0
black==24.4.2
